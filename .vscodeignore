# Files excluded from the published .vsix.
# Anything not listed here is included.

.vscode/**
.vscode-test/**
.github/**
.claude/**
agent-context/**
tests/**
test/**
src/**
esbuild.mjs
CLAUDE.md
AGENTS.md

# Test-only ESM build of the pure completion engine (consumed by node --test).
# The shipped runtime is dist/extension.js; this bundle is not needed in the .vsix.
dist/completion.mjs

.gitignore
.gitattributes

vsc-extension-quickstart.md
**/*.map
**/*.ts
**/tsconfig.json
**/.eslintrc*
**/eslint.config.*

node_modules/**
*.vsix
package-lock.json

# Ship only the published icon; D1–D5 / E1–E5 are design alternates kept in-repo.
images/**
!images/D1_256.png
