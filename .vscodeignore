# Files excluded from the published .vsix.
# Anything not listed here is included.

.vscode/**
.vscode-test/**
.github/**
.claude/**
agent-context/**
docs/**
tests/**
test/**
src/**
esbuild.mjs
CLAUDE.md
AGENTS.md

# Test-only ESM builds of the pure modules (consumed by node --test).
# The shipped runtime is dist/extension.js; these bundles aren't needed in the .vsix.
dist/*.mjs

.gitignore
.gitattributes

vsc-extension-quickstart.md
**/*.map
**/*.ts
**/tsconfig.json
**/.eslintrc*
**/eslint.config.*

node_modules/**
*.vsix
package-lock.json

# Ship only the published icon; D1–D5 / E1–E5 are design alternates kept in-repo.
images/**
!images/D1_256.png
